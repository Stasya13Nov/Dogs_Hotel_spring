spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:5432/dogHotel'
    username: postgres
    password: rootroot
  jpa:
    properties:
      hibernate:
        dialect: 'org.hibernate.dialect.PostgreSQLDialect'

  mvc:
    hidden-method:
      filter:
        enabled=true